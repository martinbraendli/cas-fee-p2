///<reference path='_reference.ts' />

module $constants {
    export module image {
        export var maxLengthA:number = 400; // max width of an uploaded image
        export var maxLengthB:number = 300; // max height of an uploaded image
    }
}